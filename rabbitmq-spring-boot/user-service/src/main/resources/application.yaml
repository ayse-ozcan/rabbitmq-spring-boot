server:
  port: 9091

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: rabbitExample
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASS}
  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASS}
